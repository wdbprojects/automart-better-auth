generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema", "strictUndefinedChecks", "driverAdapters"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
